# Core ML and Deep Learning
torch>=1.9.0
torch-geometric>=2.0.0
torchvision>=0.10.0
transformers>=4.20.0
scikit-learn>=1.0.0
numpy>=1.21.0

# NLP and Text Processing
spacy>=3.4.0
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.4.1/en_core_web_sm-3.4.1-py3-none-any.whl

# Data Processing and Analysis
pandas>=1.3.0
networkx>=2.6.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Progress and Utilities
tqdm>=4.62.0
argparse

# Collections (usually built-in, but listed for completeness)
collections-extended>=2.0.0

# JSON handling (built-in, but some may prefer ujson for performance)
# ujson>=5.0.0  # Optional: uncomment for faster JSON processing

# Optional: For t-SNE visualization in the first script
# scikit-learn already includes t-SNE, but you might want specific versions
# sklearn>=0.0  # This is a metapackage that points to scikit-learn

# For CUDA support (optional, uncomment if using GPU)
# torch-audio>=0.9.0  # If audio processing is needed
# cudatoolkit>=11.3  # For conda environments with CUDA

# Development and Testing (optional)
# pytest>=6.0.0
# black>=22.0.0
# flake8>=4.0.0